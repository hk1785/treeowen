## treeowen/src/Makevars.win
RCPP_CFLAGS = $(shell Rscript.exe --no-save --no-restore -e "cat(Rcpp:::CxxFlags())")

CXX_STD      = CXX11
PKG_CXXFLAGS = $(RCPP_CFLAGS) $(SHLIB_OPENMP_CXXFLAGS)
PKG_LIBS     = $(SHLIB_OPENMP_CXXFLAGS)
